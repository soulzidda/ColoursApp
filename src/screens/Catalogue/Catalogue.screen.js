import React from 'react';
import {Text} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

const CatalogueScreen = () => {
  return (
    <SafeAreaView>
      <Text>I am the Catalogue Screen</Text>
    </SafeAreaView>
  );
};

export default CatalogueScreen;
